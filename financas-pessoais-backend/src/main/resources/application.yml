server:
  port: 8080
spring:
  application:
    name: Financas Pessoais Backend
  datasource:
    url: jdbc:postgresql://localhost:5432/finaces
    username: vyctor
    password: vk151080
    drive-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations:
      classpath: /db/migration
  jpa:
    database: POSTGRESQL
    show-sql: true
